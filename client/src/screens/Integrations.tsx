import { Check, Github, RefreshCw, Unplug } from 'lucide-react'
import { PageHeader } from '../components/PageHeader'
import { integrations } from '../lib/demo'
export function Integrations(){return <div className="page-enter px-5 py-8 md:px-10 md:py-12 xl:px-16"><PageHeader eyebrow="Connected evidence" title="Integrations" description="External signals remain inferred until you explicitly add them to your skill bank."/><div className="divide-y border-y">{integrations.map(item=><article key={item.provider} className="grid gap-6 py-8 lg:grid-cols-[1fr_1fr] lg:items-start"><div className="flex gap-4"><div className="grid size-11 shrink-0 place-items-center rounded-full border">{item.provider==='github'?<Github size={20}/>:<span className="font-mono text-xs font-bold">LC</span>}</div><div><h2 className="section-title capitalize">{item.provider}</h2><p className="mt-1 text-sm text-muted">{item.connected?`${item.handle} · Synced ${item.lastSyncedAt}`:'Connect your profile to surface evidence-backed topic strengths.'}</p><div className="mt-4 flex gap-2">{item.connected?<><button className="button-secondary"><RefreshCw size={15}/>Sync now</button><button className="button-ghost"><Unplug size={15}/>Disconnect</button></>:<button className="button-primary">Connect {item.provider}</button>}</div></div></div><div>{item.connected?<><div className="mb-3 flex items-center justify-between"><p className="eyebrow">Last sync findings</p><span className="tag !border-warning/40 !text-warning">Inferred — not claimed</span></div><div className="space-y-2">{item.inferredSkills.map(skill=><div className="flex items-center justify-between border-b py-3" key={skill.id}><div><p className="font-semibold">{skill.name}</p><p className="text-xs text-muted">Inferred from {item.provider} activity</p></div><button className="button-secondary"><Check size={15}/>Review & add</button></div>)}</div></>:<div className="border-l pl-5 text-sm text-muted"><p className="font-semibold text-ink">Nothing is imported automatically.</p><p className="mt-2">After syncing, each inferred skill appears here for review.</p></div>}</div></article>)}</div></div>}
