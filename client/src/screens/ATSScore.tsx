import { ArrowRight, Check, X } from 'lucide-react'
import { Link } from 'react-router-dom'
import { PageHeader } from '../components/PageHeader'
import { jd } from '../lib/demo'
export function ATSScore(){const matched=jd.atsKeywords.slice(0,6),missing=jd.atsKeywords.slice(6);return <div className="page-enter px-5 py-8 md:px-10 md:py-12 xl:px-16"><PageHeader eyebrow="Version v4 · Fathom" title="ATS coverage" description="Keyword coverage is tied to this exact resume version, not your full skill bank." action={<Link className="button-secondary" to="/editor">Open version <ArrowRight size={16}/></Link>}/><div className="grid gap-12 lg:grid-cols-[.55fr_1.45fr]"><aside><div className="relative grid aspect-square max-w-64 place-items-center rounded-full" style={{background:'conic-gradient(var(--accent) 0 86%, var(--line) 86% 100%)'}}><div className="grid size-[78%] place-items-center rounded-full bg-canvas text-center"><div><p className="font-display text-6xl">86</p><p className="eyebrow mt-1">Percent covered</p></div></div></div><p className="mt-6 max-w-xs text-sm text-muted">Strong alignment. One source keyword is not represented in the approved resume evidence.</p></aside><section className="divide-y border-y"><KeywordGroup title="Matched keywords" items={matched} matched/><KeywordGroup title="Missing keywords" items={missing} matched={false}/><div className="py-7"><p className="eyebrow mb-3">Interpretation</p><p className="max-w-2xl leading-7 text-muted">Coverage measures literal language alignment. It does not reward adding unsupported claims; missing keywords should only be addressed when your skill bank contains truthful evidence.</p></div></section></div></div>}
function KeywordGroup({title,items,matched}:{title:string;items:string[];matched:boolean}){return <div className="py-7"><h2 className="section-title mb-4">{title}</h2><ul className="grid gap-2 sm:grid-cols-2">{items.map(x=><li className="flex items-center gap-3 text-sm" key={x}>{matched?<Check size={16} className="text-success"/>:<X size={16} className="text-danger"/>}{x}</li>)}</ul></div>}
