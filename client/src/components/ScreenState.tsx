import { AlertTriangle, Inbox, RotateCw } from 'lucide-react'
export function ScreenState({kind,title,detail,onRetry}:{kind:'loading'|'empty'|'error';title:string;detail:string;onRetry?:()=>void}){return <div className="grid min-h-64 place-items-center border-y py-12 text-center" role={kind==='error'?'alert':'status'} aria-live="polite"><div className="max-w-sm">{kind==='loading'?<div className="mx-auto mb-5 h-12 w-36 animate-pulse rounded bg-line"/>:kind==='error'?<AlertTriangle className="mx-auto mb-5 text-danger" aria-hidden="true"/>:<Inbox className="mx-auto mb-5 text-muted" aria-hidden="true"/>}<h2 className="section-title">{title}</h2><p className="mt-2 text-sm text-muted">{detail}</p>{onRetry&&<button onClick={onRetry} className="button-secondary mt-5"><RotateCw size={16} aria-hidden="true"/>Try Again</button>}</div></div>}
