import { AlertTriangle, CheckCircle2, LoaderCircle } from 'lucide-react'
import type { JobState } from '../lib/types'
export function AsyncProgress({state,progress,stage}:{state:JobState;progress:number;stage:string}){const done=state==='done',failed=state==='failed';return <div className="border-y bg-surface px-4 py-4" role="status" aria-live="polite"><div className="mb-2 flex items-center justify-between gap-4 text-sm"><div className="flex items-center gap-2 font-medium">{done?<CheckCircle2 className="text-success" size={17}/>:failed?<AlertTriangle className="text-danger" size={17}/>:<LoaderCircle className="animate-spin text-accent" size={17}/>}<span>{stage}</span></div><span className="font-mono text-xs text-muted">{progress}%</span></div><div className="h-1 overflow-hidden rounded-full bg-line"><div className={`h-full transition-transform duration-300 ${failed?'bg-danger':done?'bg-success':'bg-accent'}`} style={{transform:`translateX(${progress-100}%)`}}/></div></div>}
